######################################################
# Application Properties
######################################################
# Assign entries in the APPDIR array in reverse order
# of building. In other words, the directory for this
# FE should be the first entry, followed by directories
# for dependencies. Done this way so that
# ${APPDIR} refers to this FE's directory.
APPDIR=placecntr/lwp
SQLDIR=$APPDIR/build/oa.install/framework/extensions/Product/db.sql
DB2INST=db2inst9
HIDE_WAS_RUNTIME_JAR=true
SRC_REQUIRED=no

######################################################
# System Properties
#	Note: These usually don't need to be altered.
######################################################
. ${CI_COMMON_HOME}/system.properties

######################################################
# JUnit Test Properties -- PRE-Database Operations
#
# The following format can be used to add multiple
# 	directories and commands.
# 
# Format:
# 	TEST_DIR[#]=DIR
# 	TEST_CMD[#]=CMD
# 
# Values:
# 	DIR is the directory change to (i.e forum.test)
# 	CMD is the command to run in DIR (i.e bld)
#	#   is the incremental number, starting at 1,
#	    Note: Must be same for CMD and DIR
######################################################
TEST_DIR[1]=.
TEST_CMD[1]="wsbld downloadFEs"

######################################################
# Database Operation Parameters
######################################################
#RECREATEDB=true
#DBCFG_DIR=build/catalog.test/bvt/ext/conf
#DBCFG_FILE=oa-db-config.properties

######################################################
# JUnit Test Properties -- POST-Database Operations
#
# The following format can be used to add multiple
# 	directories and commands.
# 
# Format:
# 	POST_TEST_DIR[#]=DIR
# 	POST_TEST_CMD[#]=CMD
######################################################

# Run the JUnit tests
POST_TEST_DIR[1]=catalog.test
POST_TEST_CMD[1]="${CI_COMMON_HOME}/edit_build_dot_xml.sh placesCatalog build.xml"
POST_TEST_DIR[2]=catalog.test
POST_TEST_CMD[2]="wsbld -f build.xml run-unit-tests"

POST_TEST_DIR[3]=.
POST_TEST_CMD[3]="python ${CI_COMMON_HOME}/filter_junit_report.py catalog.test/TEST-com.ibm.lotus.search.catalog.test.suite.AllCatalogTests.xml"

POST_TEST_DIR[4]=.
POST_TEST_CMD[4]="${CI_COMMON_HOME}/build_junit_report.sh catalog.test"
